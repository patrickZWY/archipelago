# Future Work

- Split the graph bundle out of the main frontend chunk so the initial app load is smaller.
- Make forgot-password and reset-password failures show inline field-level feedback, the same way login and register do.
- Improve the movie search UI with keyboard navigation and a selected-result focus state.
- Add share management so a user can list and revoke previously exported graph URLs.
- Add category and weight filters to the read-only friend graph viewer so it matches the main `Explore` inspector.
- Add more API and integration coverage for friendship flows, shared graph exports, and graph access authorization.
- Add more curated catalog bundles and incremental import previews before sync.
